# ⚠️  SUPERSEDED – canonical dependency source is now pyproject.toml (Poetry).
# This file is kept only for Dockerfile.combined compatibility.
# To regenerate from Poetry: poetry export --without dev --without-hashes -f requirements.txt -o requirements-all.txt
#
# Merged requirements for all services (gateway + ingestion + evidence + caching)
# Used by Dockerfile.combined

# Web framework & server
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# Data validation
pydantic>=2.12.5
pydantic-settings>=2.0.0
python-dotenv>=1.0.1

# AI / ML
# torch must be pinned to the CPU-only wheel BEFORE sentence-transformers is
# resolved, otherwise pip resolves the full CUDA build (~2-3 GB on PyPI).
# The CPU wheel is ~120 MB and sufficient for inference-only workloads.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0
openai>=2.17.0
sentence-transformers>=5.2.2
numpy>=1.26,<3

# Vector store
faiss-cpu>=1.7.4

# Process supervisor (runs all 4 services inside one container)
supervisor>=4.2.0
