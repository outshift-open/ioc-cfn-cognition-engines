# ⚠️  SUPERSEDED – canonical dependency source is now pyproject.toml (Poetry).
# This file is kept only for Dockerfile.combined compatibility.
# To regenerate from Poetry: poetry export --without dev --without-hashes -f requirements.txt -o requirements-all.txt
#
# Merged requirements for all services (gateway + ingestion + evidence + caching)
# Used by Dockerfile.combined

# Web framework & server
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# Data validation
pydantic>=2.12.5
pydantic-settings>=2.0.0
python-dotenv>=1.0.1

# AI / ML
# fastembed uses ONNX runtime – no PyTorch/CUDA dependency, ships ~50 MB.
openai>=2.17.0
fastembed>=0.4.0
numpy>=1.26,<3

# Vector store
faiss-cpu>=1.7.4

# (langchain-openai, pandas, requests, werkzeug removed – not used at runtime)
